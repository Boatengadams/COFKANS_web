import { GitBranch } from 'lucide-react'
import { useManagerCollection, displayValue } from '../manager-live-data'

export default function Branches() {
  const { rows, loading, error } = useManagerCollection('branches')
  return <div style={{ padding: '24px 24px 48px' }}><div style={{ marginBottom: 16 }}><p style={{ margin: '0 0 4px', color: 'var(--muted-foreground)', fontSize: 11, fontWeight: 700, letterSpacing: '0.08em', textTransform: 'uppercase' }}>Firebase collection: branches</p><h1 style={{ margin: 0, color: 'var(--foreground)', fontSize: 22 }}>Branches</h1></div><div style={{ overflow: 'auto', border: '1px solid var(--border)', borderRadius: 12, background: 'var(--card)' }}><table style={{ width: '100%', minWidth: 620, borderCollapse: 'collapse', fontSize: 12 }}><thead><tr>{['Name', 'City', 'Region', 'Address', 'Status'].map(column => <th key={column} style={{ padding: '11px 14px', textAlign: 'left', color: 'var(--muted-foreground)', background: 'var(--muted)', borderBottom: '1px solid var(--border)' }}>{column}</th>)}</tr></thead><tbody>{loading ? <tr><td colSpan={5} style={{ padding: 40, textAlign: 'center', color: 'var(--muted-foreground)' }}>Loading branches…</td></tr> : error ? <tr><td colSpan={5} style={{ padding: 40, textAlign: 'center', color: 'var(--muted-foreground)' }}>{error}</td></tr> : rows.length === 0 ? <tr><td colSpan={5} style={{ padding: 40, textAlign: 'center', color: 'var(--muted-foreground)' }}><GitBranch size={20} style={{ display: 'block', margin: '0 auto 8px' }} />No branch records in Firebase.</td></tr> : rows.map(row => <tr key={row.id} style={{ borderBottom: '1px solid var(--border)' }}><td style={{ padding: 14, color: 'var(--foreground)', fontWeight: 600 }}>{displayValue(row.name)}</td><td style={{ padding: 14, color: 'var(--muted-foreground)' }}>{displayValue(row.city)}</td><td style={{ padding: 14, color: 'var(--muted-foreground)' }}>{displayValue(row.region)}</td><td style={{ padding: 14, color: 'var(--muted-foreground)' }}>{displayValue(row.address)}</td><td style={{ padding: 14, color: 'var(--muted-foreground)' }}>{displayValue(row.active ?? row.isActive)}</td></tr>)}</tbody></table></div></div>
}
